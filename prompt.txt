使用shell脚本，默认从当前文件夹开始，不断的遍历文件夹，
如果当前文件夹是一个git 仓库，并且配置了remote，显示日志代表正在处理当前文件夹，并执行git fetch 命令。
通过git status 判断，
如果可以执行fast-forward，用绿色打印日志并执行fast-forwand。
如果不能够执行fast-forward，就使用红色打印日志。
如果包含未提交的代码或者等待push 的提交，如果有就用蓝色打印日志。
如果没有配置remote，使用紫色打印日志。
如果当前文件夹是git 仓库，不再遍历该仓库的子文件夹。